import classNames from "classnames/bind";
import styles from "./AlbumList.module.scss";

const cx = classNames.bind(styles);
function AlbumList({ trackList }) {
  return <div></div>;
}

export default AlbumList;
